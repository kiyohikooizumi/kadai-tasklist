<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.id, task %> : 
      <p>ステータス:<%= task.status %></p> <p>タスク:<%= task.content %></p>
    </li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>